%function placeholder = Fractal(argument)
%colormap = blanks(150,150);
%colormap = reshape(blanks(22500),150,150);
colormap = blanks(22500);

z = zeros(1, 22500);


x=linspace(.15,.55,150);
y=linspace(-.15,.15,150);

for i=1:150
    for j=1:150
       z(i) =  complex(x(i), y(j));
    end
end
disp(

%{
for i=1:150
    for j=1:150
        k = 1;
        z(k) = complex(x(i), y(j));
        colormap(k)=Newton(z(k));
        k = k + 1;
    end 
end
%}
 

